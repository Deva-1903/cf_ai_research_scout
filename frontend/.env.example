# Copy this to .env.local for local overrides.
# In dev, the Vite proxy handles /api → localhost:8787 automatically,
# so VITE_API_URL is not required locally.

# For production builds, set this to your deployed worker URL:
# VITE_API_URL=https://cf-ai-research-scout-worker.your-subdomain.workers.dev
